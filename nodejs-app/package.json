{
  "name": "db-agent-node",
  "version": "0.1.0",
  "description": "DB Agent — Node.js/Express prototype of the same text-to-SQL agent as the Streamlit app, for UI comparison.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3vectors": "^3.1095.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "openai": "^4.67.0"
  }
}
